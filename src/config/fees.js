module.exports = {
  // === تنظیمات واریز (Deposit) ===
  DEPOSIT: {
    MIN_AMOUNT: 1, // حداقل واریز مجاز (ONE)
    FEE_PERCENT: 0.01, // کارمزد واریز (معمولاً 0 است)
    MIN_FEE: 0,
    MAX_FEE: 0
  },

  // === تنظیمات برداشت (Withdraw) ===
  WITHDRAW: {
    MIN_AMOUNT: 5, // حداقل مقدار قابل برداشت (ONE)
    FEE_PERCENT: 0.1, // کارمزد برداشت (مثلاً 1 درصد)
    MIN_FEE: 1, // حداقل کارمزد (مثلاً اگر 1 درصد شد 0.001، ما 0.1 می‌گیریم)
    MAX_FEE: 20 // حداکثر کارمزد (سقف کارمزد)
  }
};